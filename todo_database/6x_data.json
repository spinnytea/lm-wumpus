{"name":"converage config.init","description":"istanbul launches the tests in a new node instance\nas such, we can't \"just run\" config.init\nright now, the workaround is to call what we want in the config file itself, but it should probably be configured in the gulp task","resolution":"config.onInit"}