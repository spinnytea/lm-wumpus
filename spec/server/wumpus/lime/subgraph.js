'use strict';
/* global describe, it */
//var expect = require('chai').expect;

//
// this is a unit test of subgraph
// I just need a very large integrated environment to have enough data for it
//

describe('subgraph', function() {
  it.skip('search');

  it.skip('match');
});