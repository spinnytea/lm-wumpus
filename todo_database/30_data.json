{"name":"Where do we filter?","description":"Client Side? during expand? data.list = data.list.filter(function(task) { return task.status !== 'h'; });\nServer? during subgraph.search? after subgraph.search?\n\nnot on the client side, that's dumb","resolution":"Server. Always.\nIf we can't, then that means that our search criteria isn't good enough and it needs to be improved.\nSure, this project isn't the star use case of why the subgraph stuff was engineered. But if anything can't be done with a search directly, then it needs to be addressed in server code."}