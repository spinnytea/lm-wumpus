{"name":"Where do we filter?","description":"Client Side? during expand? data.list = data.list.filter(function(task) { return task.status !== 'h'; });\nServer? during subgraph.search? after subgraph.search?"}